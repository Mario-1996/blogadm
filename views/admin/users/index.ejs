<%- include ('../../partials/header.ejs') %>
<%- include ('../../partials/navbar.ejs') %>

    <div class="container">
        <hr>
        <h2>Usuários</h2>
        <a class="btn btn-outline-success" href="/admin/users/create" > Nova Usuário</a>
        <hr>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>email</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
              <% users.forEach(user => { %>
                <tr>
                    <th><%= user.id%></th>
                    <th><%=user.email%></th>
                    <th>
                        <a class="btn btn-warning" href="/admin/categories/edit/<%= users.id %>" title="Editar">Editar</a>
                        <form method="POST" action="/categories/delete" style="display: inline;" onsubmit="confirmarDeletar(event, this)">
                            <input type="hidden" name="id" value="<%= users.id %>">
                            <button class="btn btn-danger" title="Excluir/Deletar">Deletar</button>
                        </form>
                    </th>
                </tr>
              <% }) %>
            </tbody>
        </table>
    </div>
<%- include ('../../partials/footer.ejs') %>
<script>
    function confirmarDeletar(event, form) {
        event.preventDefault();
        var decision = confirm("Você deseja deletar essa Categoria ?")
        if(decision) {
            form.submit();
        }
    }
</script>